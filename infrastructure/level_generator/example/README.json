* Pull down alpine:latest
* start syslogd
* install openssh and start it
* add your publickey to root's authorized keys
* ssh to the container with your key
* run checkLevel-linux-amd64 on the container (either directly attached or SSH)